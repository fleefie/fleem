-- This file loads AFTER lazy has been fully initialized.
-- If something can be done elsewhere, do it that way!
-- Everything here is a dirty hack. But sometimes thats
-- just how we love it, eh?

-- Colorscheme setup, it's now safe to add it
vim.cmd.colorscheme("nordic")
