-- Telescope settings. I like my telescope to be bigger so I use these.

return {
  defaults = {
    sorting_strategy = "ascending",
    layout_config = {
      horizontal = {
        preview_width = 0.6,
        preview_cutoff = 75,
        width = 0.8,
        height = 0.8,
        prompt_position = "top"
      },
    },
  },
}
